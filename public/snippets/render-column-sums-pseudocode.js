const renderColumnSums = function() {

  const fetchCellValues = function() {
    //  for (each col index up to the num of cols) {
      //  fetch all the cell values for that column from the model
    //  }
  }

  const filterNaNValues = function() {
    //   filter out the non-numeric values from this list using array.filter()

    //   add up the filtered values in the list using array.reduce()
  }

  const findFooter = function() {
    //   find the footer element for this column

   //   set its text content = sum
  }
};